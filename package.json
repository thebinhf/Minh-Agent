{
  "name": "minh-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Minh-Agent monorepo: task board (Node) and Bybit market tracker (Bun).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm ci --prefix apps/task-board && cd apps/bybit-ws-tracker && bun install",
    "dev": "npm run dev --prefix apps/task-board",
    "dev:task-board": "npm run dev --prefix apps/task-board",
    "dev:bybit": "cd apps/bybit-ws-tracker && bun run start",
    "build": "npm run build --prefix apps/task-board",
    "start": "npm run start --prefix apps/task-board",
    "typecheck": "npm run typecheck --prefix apps/task-board",
    "lint": "npm run lint --prefix apps/task-board",
    "test": "npm run test --prefix apps/task-board && cd apps/bybit-ws-tracker && bun test",
    "test:task-board": "npm run test --prefix apps/task-board",
    "test:bybit": "cd apps/bybit-ws-tracker && bun test"
  }
}
