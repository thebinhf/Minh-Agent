{
  "name": "bybit-ws-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bybit public linear WebSocket market tracker (Bun + SQLite). No API keys, no trading.",
  "scripts": {
    "start": "bun run src/index.ts",
    "query": "bun run src/query.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
