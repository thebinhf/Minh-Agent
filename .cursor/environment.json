{
  "name": "Minh-Agent",
  "install": "command -v bun >/dev/null 2>&1 || curl -fsSL https://bun.sh/install | bash; export PATH=\"$HOME/.bun/bin:$PATH\"; bun install",
  "terminals": [
    {
      "name": "minh-agent",
      "command": "export PATH=\"$HOME/.bun/bin:$PATH\" && bun run start"
    }
  ],
  "ports": [
    {
      "name": "bybit-tracker",
      "port": 43180
    },
    {
      "name": "paper",
      "port": 43181
    }
  ]
}
